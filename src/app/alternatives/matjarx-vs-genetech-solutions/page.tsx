import type { Metadata } from 'next'
import ComparisonContent, { type ComparisonContentShape } from '@/components/comparisons/ComparisonContent'
import { getMergedContent } from '@/lib/marketing-content'
import { RIVAL_DATA } from '@/lib/comparison-data'

const rival = RIVAL_DATA['genetech-solutions']

export const metadata: Metadata = {
  alternates: { canonical: '/alternatives/matjarx-vs-genetech-solutions' },
  title: `MatjarX vs ${rival.name}`,
  description: rival.intro[0],
}

export const revalidate = 60

export default async function Page() {
  const content = await getMergedContent<ComparisonContentShape>('comparisons/genetech-solutions')
  return <ComparisonContent rivalKey="genetech-solutions" content={content} />
}
